import numpy as np

# 行列AとBを定義
A = np.array([[0, 1],
              [2, 3],
              [4, 5]])

B = np.array([[0, 1, 2, 3],
              [4, 5, 6, 7]])

# 行列の積を計算
C = np.dot(A, B)

# 結果と最大値を表示
print("行列の積の結果:")
print(C)
print("行列の要素の最大値:")
print(C.max())